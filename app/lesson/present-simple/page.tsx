"use client";import {useState} from "react";import Link from "next/link";
const s=[["Present Simple คืออะไร","ใช้พูดถึงกิจวัตร ข้อเท็จจริง และสิ่งที่เกิดขึ้นเป็นประจำ","Subject + Verb 1"],["I / You / We / They","ใช้คำกริยารูปปกติ","I/You/We/They + Verb 1"],["He / She / It","คำกริยาส่วนใหญ่เติม s หรือ es","He/She/It + Verb-s/es"],["ประโยคปฏิเสธ","ใช้ do not หรือ does not","Subject + do/does not + Verb 1"]];
export default function Lesson(){const[i,setI]=useState(0);const[d,setD]=useState<number[]>([]);const x=s[i];const pct=Math.round(d.length/s.length*100);return <main><header className="lessonTop"><Link href="/student">← Dashboard</Link><strong>Present Simple</strong><span>{pct}%</span></header><div className="progress"><div style={{width:pct+"%"}}/></div><section className="lessonShell"><aside className="lessonNav">{s.map((a,n)=><button className={(n===i?"active ":"")+(d.includes(n)?"done":"")} onClick={()=>setI(n)} key={a[0]}><span>{d.includes(n)?"✓":n+1}</span>{a[0]}</button>)}</aside><article className="lessonContent"><span className="eyebrow">บทที่ {i+1} จาก {s.length}</span><h1>{x[0]}</h1><p>{x[1]}</p><div className="formula">{x[2]}</div><h2>ตัวอย่าง</h2><div className="example">I go to school every day.</div><div className="example">She reads every night.</div><div className="lessonActions"><button className="button secondary" disabled={i===0} onClick={()=>setI(i-1)}>← ก่อนหน้า</button><button className="button primary" onClick={()=>{setD(p=>p.includes(i)?p:[...p,i]);if(i<s.length-1)setI(i+1)}}>เข้าใจแล้ว ✓</button><button className="button secondary" disabled={i===s.length-1} onClick={()=>setI(i+1)}>ถัดไป →</button></div></article></section></main>}