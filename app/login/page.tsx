"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {BrandHeader} from "@/components/BrandHeader";
export default function Login(){const r=useRouter();const[role,setRole]=useState<"student"|"teacher">("student");function go(e:React.FormEvent){e.preventDefault();localStorage.setItem("gfa-demo-role",role);r.push(role==="teacher"?"/teacher":"/student")}return <main className="page"><BrandHeader/><section className="authWrap"><form className="authCard" onSubmit={go}><span className="eyebrow">WELCOME BACK</span><h1>เข้าสู่ Green Freedom Academy</h1><p>เปิด Demo Mode ได้ทันที และเตรียมพร้อมเชื่อม Supabase Auth</p><div className="roleTabs"><button type="button" className={role==="student"?"active":""} onClick={()=>setRole("student")}>นักเรียน</button><button type="button" className={role==="teacher"?"active":""} onClick={()=>setRole("teacher")}>ครู</button></div><label>อีเมล<input type="email" defaultValue="demo@gfa.app"/></label><label>รหัสผ่าน<input type="password" defaultValue="demo1234"/></label><button className="button primary full">เข้าสู่ระบบ Demo</button></form></section></main>}